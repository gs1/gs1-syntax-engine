# Biome conflicts with ESLint and Prettier (Super Linter warns)
VALIDATE_BIOME_FORMAT=false
VALIDATE_BIOME_LINT=false

# Project uses tab indent and K&R braces; covered by own CI
VALIDATE_CLANG_FORMAT=false
VALIDATE_CPP=false

# Restore fails for WPF projects on Linux
VALIDATE_DOTNET_SLN_FORMAT_ANALYZERS=false
VALIDATE_DOTNET_SLN_FORMAT_STYLE=false

# google-java-format conflicts with Checkstyle
VALIDATE_GOOGLE_JAVA_FORMAT=false

# Prettier conflicts with existing formatters for these types
VALIDATE_HTML_PRETTIER=false
VALIDATE_JSON_PRETTIER=false
VALIDATE_MARKDOWN_PRETTIER=false

# Black conflicts with Ruff Format; Pylint duplicates Ruff
VALIDATE_PYTHON_BLACK=false
VALIDATE_PYTHON_PYLINT=false

# Prettier conflicts with existing formatters for these types
VALIDATE_TYPESCRIPT_PRETTIER=false
VALIDATE_YAML_PRETTIER=false
